# 2026-09-20：Claude／Codex 共用 Skill 接入

## 来源与目标

负责人希望 laptop 按 AI、UI、Ability 分模块并行研发，使用相同工作方法；所有实质修改沉淀为 Skill 或文档。本次明确要求把方案迭代进 bootstrap-project-workflow。

## 变更

- 保留首次初始化仅两个文件，补齐拆解产物中的 Codex 薄入口。
- 新增接入与维护模式，以普通相对路径入口复用 .claude 正文；处理冲突、已有定制、元数据更新和已授权范围。
- 明确任务记录、模块知识、新模块与共同规则的不同回写落点。
- 更新 README、设计、Skill 触发描述及 Codex 展示信息。
- 安装器行为不变；明确拉取仓库不等于更新已安装副本。

## 验证

- 官方 quick_validate.py 通过；校验器需要的 PyYAML 通过 uv 隔离环境提供，未加入项目依赖。
- 两个安装器分别在临时 USERPROFILE 安装成功，全部文件（含 references）与源哈希一致。
- 两个安装器再次运行均拒绝覆盖，安装内容保持不变。
- 从参考文档提取薄入口模板，验证它解析到 .claude 正文，正文引用解析到自身 references；复制整个临时项目到另一个目录后仍通过。
- README、设计与 Skill 的本地文档链接、openai.yaml 均通过检查。
- git diff --check 通过。
- 未运行真实 laptop 或新 Claude／Codex 会话，未验证自动匹配、真实接入执行的幂等性或 UE 流程；不将文件检查等同于这些结果。
- 未修改真实安装目录或业务项目；用户已有授权只用于本工具仓库迭代与发布。
